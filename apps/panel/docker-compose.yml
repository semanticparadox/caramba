version: '3.8'

services:
  panel:
    build: .
    container_name: caramba-panel
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./exarobot.db:/app/exarobot.db
      - ./config:/app/config
      - ./logs:/app/logs
    env_file:
      - .env
    network_mode: bridge
