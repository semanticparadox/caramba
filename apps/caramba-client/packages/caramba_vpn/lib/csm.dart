/// Публичная поверхность проверяющего CSM/1 для приложения.
///
/// Отдельная точка входа, а не часть `caramba_vpn.dart`: контракт туннеля и
/// проверяющий манифестов это две независимые поверхности, и импорт одной не
/// должен тянуть другую. Внутренние модули под `src/csm/` не импортируются
/// напрямую ни приложением, ни тестами.
library;

export 'package:caramba_vpn/src/csm/csm.dart';
