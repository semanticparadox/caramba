# Автономный ultracode-промпт (вставить в НОВОЙ сессии)

Скопируй всё, что ниже разделителя, в чистую сессию. Прогон долгий и автономный,
твоё участие не требуется до финального отчёта.

---

ultracode. Продолжаем Caramba Connect (B2B-разворот VPN exarobot). Открой проект
/Users/smtcprdx/Documents/Projects/caramba. Это ПРЕДАВТОРИЗОВАННЫЙ долгий
автономный прогон: НЕ останавливайся и НЕ спрашивай меня между фазами, доведи всё
до конца сам, и только в самом конце дай отчёт.

СНАЧАЛА прочитай контекст (обязательно, до любых действий):
- Память автоподгружается; перечитай файлы: memory/caramba-connect-platform.md,
  memory/caramba-client-app.md, memory/no-ai-slop.md.
- docs/CARAMBA-CONNECT-PLAN.md — ИСПОЛНЯЕМАЯ СПЕЦИФИКАЦИЯ (источник истины: тиры,
  контракты, протоколы, пофазная разбивка P0..P7, владение файлами, acceptance).
- apps/caramba-client/ANTI-SLOP.md, docs/AMNEZIAWG.md, apps/caramba-client/
  INTEGRATION.md — действующие правила и состояние.

ОПЕРАЦИОННЫЕ ПРАВИЛА (соблюдать всегда):
1. Бренд: user-facing по умолчанию = "Caramba Connect"; exarobot = тенант #1;
   per-tenant бренды = операторская настройка. Код-иды остаются caramba (директории
   caramba-*, Go-модуль github.com/semanticparadox/caramba/..., Dart-пакет
   caramba_client, плагин caramba_vpn, каналы com.caramba/vpn). Константа
   profile.CarambaSelector="CARAMBA" — контракт панель<->клиент, НЕ менять.
2. Анти-слоп: нет фиолета/градиентов/глассморфизма; цвет = только статус
   подключения; иконки Lucide (не эмодзи); моно только для тех-данных; БЕЗ em-dash
   в user-facing строках и прозе доков (em-dash ок только в код-комментариях); без
   слоп-слов; без маркетинг-воды.
3. В песочнице НЕТ тулчейнов (go/cargo/flutter). Весь код пишется и выверяется
   статически: ничего не компилировать/биндить/запускать. Код держать compile-ready
   под стиль репозитория. Панель (Rust) = источник истины контрактов, клиенты
   подгоняются.
4. Не ломать ~20 живых юзеров и прод-потоки.

ЧТО СДЕЛАТЬ: исполни фазы P0..P7 из docs/CARAMBA-CONNECT-PLAN.md ПОСЛЕДОВАТЕЛЬНО,
каждую как ОТДЕЛЬНЫЙ ultracode-workflow по паттерну recon (только чтение) -> build
(непересекающиеся директории по владению из плана) -> состязательное ревью (>=2
линзы: логика/контракт + бренд/анти-слоп; для фазы лицензий P4 добавь линзу
безопасности) -> fix (закрой все critical+major находки). Следующую фазу запускай
ТОЛЬКО после того, как fix-агент закрыл критичные находки предыдущей. Тебя будут
переподнимать уведомления о завершении фоновых workflow — на каждом продолжай
цепочку дальше сам, пока все 8 фаз не пройдены. В промптах-шаблонов workflow
избегай обратных кавычек.

КЛЮЧЕВЫЕ РЕШЕНИЯ (уже зафиксированы, см. план, не переспрашивай):
- Тенантность = self-hosted инстансы + тонкий лиценз-сервер. Ноды приносит
  оператор. Тиры: Free (2 ноды, 100 юзеров, биллинг выкл, брендинг нельзя,
  показывает рекламу Caramba Connect, админ вручную выдаёт конфиги) и Pro (высокие
  лимиты, биллинг и брендинг можно, без рекламы; цена оператору: 3 мес $40 / 6 мес
  $70 / 12 мес $120). Константы лимитов вынести в одно место.
- Клиент универсальный: режим сырой подписки (sing-box/clash/v2ray/URI, без
  аккаунта) + режим панель-аккаунта (enrollment-ссылка carambaconnect://enroll,
  аккаунт обязателен). Мульти-профиль; активный профиль задаёт подключение и тему.
- Неоплаченный панель-аккаунт может получить onboarding-трафик (панель-настройка)
  чтобы дойти до Telegram и оплатить.
- Протоколы: все актуальные (VLESS Reality/WS/HU/gRPC, VMess, Trojan, Shadowsocks/
  ShadowTLS, Hysteria2, TUIC, AmneziaWG/WireGuard, NaiveProxy).
- Сторы: целиться в прохождение Apple/Play (ориентир Hiddify/Streisand/v2rayNG),
  без динамического кода, корректные VPN-права, privacy-манифест, апселл без
  сторонних трекеров; sideload как запас. Аудит в P7.

В КОНЦЕ (после P7): создай docs/CARAMBA-CONNECT-STATUS.md с честным разбором — что
сделано статически по каждой фазе и что ТРЕБУЕТ моих живых тулчейнов/железа/подписи/
хостинга лиценз-сервера (список из раздела 9 плана). Обнови память
(caramba-connect-platform: что построено vs осталось). Дай мне финальный отчёт
на русском, без em-dash, без слоп-слов.

Бюджет не жалей — задача: максимально полная и корректная статическая реализация
всех фаз, состязательно проверенная.
